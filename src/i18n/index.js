/**
 * i18n/index.js — экспорт словарей переводов
 *
 * ПОЧЕМУ отдельный файл?
 * - Единая точка импорта для всех переводов
 * - Легко добавить новый язык (просто добавить import и экспорт)
 * - Контекст импортирует только этот файл, не все JSON напрямую
 *
 * Архитектурная роль:
 * Агрегатор всех словарей. LanguageContext импортирует отсюда
 * объект translations со всеми доступными языками.
 */

import ru from "./ru.json";
import en from "./en.json";

// === СЛОВАРИ ПЕРЕВОДОВ ===
// ПОЧЕМУ объект, а не массив?
// - Быстрый доступ по коду языка: translations['ru']
// - Легко проверять существование языка: 'ru' in translations
export const translations = {
  ru,
  en,
};

// === СПИСОК ДОСТУПНЫХ ЯЗЫКОВ ===
// ПОЧЕМУ массив? Используется для рендеринга переключателя языка
export const availableLanguages = ["ru", "en"];

// === ЯЗЫК ПО УМОЛЧАНИЮ ===
export const defaultLanguage = "ru";

export default translations;
